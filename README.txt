mở cmd prompt 
cd Dia\Chi\Thu\Muc
sau đó copy dòng này vào để tải module cần thiết:
pip install -r requirements.txt

Lưu ý frontend: chạy file index2.html và detail2.html (mới cập nhật lại)

vào app.py vào chạy
hoặc dùng
python app.y

link để vào web:
http://localhost:5000/


để chạy được Ai 
đầu tiên tạo một file tên là ".env"
lên https://aistudio.google.com/app/api-keys
tạo API key mới và cho vào file ".env.
